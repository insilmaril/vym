vym.clearConsole();
m1 = vym.currentMap();

m1.initIterator("foobar");
print (m1.getHeadingPlainText() );

while (m1.nextIterator("foobar") )
{
    print ("Selected branch: " + m1.getHeadingPlainText() );
}
